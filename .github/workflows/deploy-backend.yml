name: Deploy Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Deploy to Render
        # Uncomment and add RENDER_DEPLOY_HOOK secret
        # run: |
        #   curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          echo "🚀 Backend build successful!"
          echo "📝 To enable auto-deploy:"
          echo "   1. Go to GitHub → Settings → Secrets → Actions"
          echo "   2. Add RENDER_DEPLOY_HOOK"
          echo "   3. Uncomment the curl command above"
          echo "   OR connect backend repo directly to Render/Railway"
